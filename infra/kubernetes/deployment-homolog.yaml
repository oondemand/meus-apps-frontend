apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-frontend-homolog
  namespace: central-oondemand-homolog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-frontend-homolog
  template:
    metadata:
      labels:
        app: auth-frontend-homolog
    spec:
      containers:
        - name: auth-frontend-homolog-container
          image: ghcr.io/oondemand/auth-frontend:${IMAGE_TAG}-homolog
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: "256Mi"
              cpu: "0.5"
            limits:
              memory: "4096Mi"
              cpu: "4"
